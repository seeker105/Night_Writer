gem 'minitest', '~> 5.2'
require 'minitest/autorun'
require 'minitest/pride'

require 'pry'
require 'braille_translator'

class BrailleTranslatorTest < MiniTest::Test

test it can map a letter to a braille characters

test it can map multiple letters to braille characters

test it can read in a single braille character

test it can print a single braille character

test it can read in multiple braille characters

test it reads in braille characters that wrap to the next line

test it reads in braille characters that wrap to multiple lines

test it reads in capital letters

test it can print multiple braille characters

test it wraps the braille text after 80 characters

test it can map a single braille character to a letter

test it can map multiple braille characters to letters




end
